server:
  port: 9001
        
spring:
  application:
    name: service-hi
  cloud:
    config:
      name: application
      label: master
      profile: ${spring.application.name}
      discovery:
        enabled: true
        service-id: config-server
      
logging:
  file: "target/${spring.application.name}.log"
