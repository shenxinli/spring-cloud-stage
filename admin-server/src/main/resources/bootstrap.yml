server:
  port: 8787
  
spring:
  application:
    name: admin-server
  cloud:
    config:
      name: application,datasource
      label: master
      profile: ${spring.application.name},base-config
      discovery:
        enabled: true
        service-id: config-server
logging:
  file: "target/${spring.application.name}.log"