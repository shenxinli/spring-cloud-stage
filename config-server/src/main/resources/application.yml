---
spring:
  cloud:
    config:
      enabled: true
      name: boorstrip,application,demo
      label: dev
      server:
        git:
          uri: https://github.com/shenxinli/config-repo
          search-paths: "{profile}"
          username: shenxinli
          password: chinaway2014
          basedir: /data
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
