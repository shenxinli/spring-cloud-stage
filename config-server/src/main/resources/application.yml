---
spring:
  cloud:
    config:
      enabled: true
      name: application
      label: master
      server:
        git:
          uri: https://github.com/shenxinli/config-repo
          search-paths: "{profile}"
          username: shenxinli
          password: chinaway2014
          basedir: /data
