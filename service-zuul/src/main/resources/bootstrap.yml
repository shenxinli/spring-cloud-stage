server:
  port: 8080

spring:
  application:
    name: service-zuul
  cloud:
  cloud:
    inetutils:
      preferred-networks: 172.18.170.
    config:
      name: application
      label: master
      profile: ${spring.application.name},global-config
      discovery:
        enabled: true
        service-id: config-server

logging:
  file: "target/${spring.application.name}.log"
